// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  client_name: "COMVERSE",
  // backend_url: "http://localhost:8000",
  backend_url: "https://staging-backend.comverseglobal.com",
  logistic_hub_backend_url: "https://logistic-hub-backend.alchemative.net",
  // backend_url : "https://7232-119-73-120-105.in.ngrok.io",
  client_img_folder: "comverse",
  currency: "PKR"
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
